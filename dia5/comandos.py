"""
CREACION DE PYOYECTO

python -m venv onlyflans ---> creamos el entorno virtual

source onlyflans/Scritpt/activate ----> nos posicionamos en el proyecto

python -m django version --> para ver la version de django 

pip list ----> vemos la lista de ls paquetes 

pip install django ---> instala django y dependencias 
pip install django==3.2.4 u otro instalada las versiones que buscamos django==version que buscamos

django-admin startproject onlyflan  -----> generamos el proyecto

cd onlyflan --> entramos al proyecto

python manage.py runserver ----> proyecto ejecutado en HTML

deactivate --->  desactivas entorno y proyecto

"""